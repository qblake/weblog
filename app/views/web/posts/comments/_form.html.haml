= simple_form_for(@comment, url: post_comments_path(@post), html: { class: 'form-horizontal' }) do |f|
  = f.input :commenter
  = f.input :body
  = f.input :parent_id, as: :hidden

  .form-actions
    = f.button :submit, class: 'btn-primary'
